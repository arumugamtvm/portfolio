(function () {
  'use strict';
  /* Intentionally empty — this site is built to be inspection- and agent-friendly.
     Previous builds shipped a DevTools lockscreen, a `debugger` trap, console
     detection, and blocked right-click / text-selection / Ctrl+U/S/P. All of that
     fought the very audience this portfolio courts (developers and AI agents), so
     it has been removed. Right-click uses the themed quick-menu defined in script.js. */
})();
